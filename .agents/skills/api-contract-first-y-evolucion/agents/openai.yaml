interface:
  display_name: "API contract-first"
  short_description: "Review HTTP operations before implementation"
  default_prompt: "Use $api-contract-first-y-evolucion to review this HTTP operation before implementation."
