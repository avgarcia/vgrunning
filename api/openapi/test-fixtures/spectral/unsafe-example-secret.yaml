openapi: 3.1.1
info: { title: Invalid, version: 0.0.1 }
servers: [{ url: /api }]
security: []
components:
  schemas:
    Credentials:
      type: object
      additionalProperties: false
      properties:
        accessToken:
          type: string
          example: sk-real-looking-secret
paths: {}
