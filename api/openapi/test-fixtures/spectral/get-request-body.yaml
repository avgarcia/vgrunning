openapi: 3.1.1
info: { title: Invalid, version: 0.0.1 }
servers: [{ url: /api }]
paths:
  /sessions:
    get:
      operationId: listSessions
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses: { '200': { description: OK } }
