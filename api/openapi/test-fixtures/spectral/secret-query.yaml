openapi: 3.1.1
info: { title: Invalid, version: 0.0.1 }
servers: [{ url: /api }]
paths:
  /sessions:
    get:
      operationId: listSessions
      parameters:
        - in: query
          name: token
          schema: { type: string }
      responses: { '200': { description: OK } }
